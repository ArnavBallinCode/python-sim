# Contributing

Create focused changes, add tests for public behavior, and run `python -m pytest` before opening a pull request. Keep the runtime dependency-free where practical and preserve deterministic behavior.
