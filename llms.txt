# python-sim

python-sim is a local, deterministic simulation platform for AI agents and Python workflows. Use `Simulation` to create a stateful world with Gmail, GitHub, Slack, Calendar, and Stripe services. Use `clock`, `events`, `snapshot`, `restore`, `save`, and `load` to control and inspect test state.
